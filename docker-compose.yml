version: "2"

services:
    php:
        image: kiwilightweight/php-fpm53
        ports:
            - "127.0.0.1:9081:9000"
        volumes:
            - ./data:/var/www/html
        restart: unless-stopped
#    cron:
#        image: kiwilightweight/cron-jessie
#        links:
#            - php
#        volumes:
#            - ./data:/var/www/html
#        restart: unless-stopped
